# 2026-07-08

## stark-repo-analyzer · PLAN_COMPLIANCE.md 诚实化修正（T07 stub）
- 用户要求把 `references/PLAN_COMPLIANCE.md` 改回诚实口径，明确 T07 外部调研（`--research` / `03-research.md`）为**已知限制 stub**：代码里 `write_research()` 硬编码 `status: SKIP`，不产出真实 5 章节，非真正"已实现"。
- 改了 8 处：结论状态块（标 * T07 stub）、结论补齐清单（7 项→6 项补齐 + T07 stub）、覆盖率总览表（延后实现 14→13 实现+1 stub）、§3 T07 行（状态 已实现→已知限制 stub，说明重写）、§5「原 7 项缺口」清单与结论（全 PASS→PASS* 除 T07）。
- 不动代码（用户明确"不写代码"）。其余 6 项能力 + LLM-judge/Mermaid/外链离线降级仍计 PASS。T07 真实联网调研实现仍待办（需有网环境 + WebSearch/codex 能力）。

## stark-repo-analyzer 重构规划阶段完成（待用户 review）
- 用户反馈 12 项问题，走标准 SOP 规划阶段，团队 software-stark-refactor
- PM 许清楚 → 增量 PRD：12 项归并为 10 个需求(REQ-01~10)，P0 六项/P1 四项。文档 docs/goals/refactor-prd-2026-07-08.md
- 架构师高见远 → 根因分析+方案+任务分解(1229行，file:line 全核实)。文档 docs/goals/refactor-architecture-2026-07-08.md
- 关键方案：
  - REQ-10 拆分：repo_analyzer.py(2746行)→11 个 analyzer_*.py + 入口<200行，_LOADER/_TOKEN_COLLECTOR 放 analyzer_common.py 模块级单例，无循环依赖
  - REQ-06 重组：analysis→.stark-repo-analyzer，分层 reports/(人类)/data/(agent)/diagnostics/(诊断)/logs/(日志)/slices/drafts/agent-runs/acceptance，去编号前缀
  - REQ-05 agent-mode 默认 deterministic→codex，预检 codex 不可用自动降级+记 agent_mode_degraded
  - REQ-02 新增 preflight_check(Python/git/npx 必需, tree-sitter 可选, codex 条件必需)
  - REQ-04 改 timed_stage→ProgressReporter(计时+stdout进度[N/M]+写日志)
  - REQ-07 LogWriter 写 logs/run-YYYYMMDD-HHMMSS.md
  - REQ-09 名片 5KB→10KB(+8字段)
  - REQ-03 llm_judge DEFAULT_MODEL 改空(用codex默认)+失败输出模型名+stderr摘要
- 任务 T01-T12：Phase0(拆分先行,T01-T05)→Phase1(T06 llm-judge/T07 名片,可并行)→Phase2(T08 目录重组/T09 预检+agent默认/T10 进度日志)→Phase3(T11 PERF定位/T12 SKILL.md)
- 关键排序：REQ-10 先于 REQ-06（先拆再改路径，diff 更小）
- 待用户 review 通过 → 工程师寇豆码实施 → QA 严过关回归

## codex 对抗性审查受阻 + 重构续跑（续会话）
- 用户确认方案通过，启动工程师寇豆码后台实施 T01-T12（首次拉起后遭遇会话摘要中断）
- 用户要求 /code-review 对抗性审查 → 切到 /codex setup → 用 codex 跑对抗性审查
- codex setup: ready=true，但 auth 为 apiKey(provider=codexzh, verified=false)
- codex adversarial-review 失败：401 Unauthorized「无效的令牌」@ https://api.codexzh.com/v1/responses，5 次重连全挂，20s 即 Turn failed。即 codexzh 通道 API key 失效，需用户 codex login 或换有效 key
- 关键发现：T01-T12 重构根本没落地——repo_analyzer.py 仍 2746 行，scripts/analyzer_*.py 只有 analyzer_common.py 一个(262行，T01 半成品)。上轮工程师被摘要中断
- 用户决策：先补齐重构再审 + 先修 codex 令牌
- 重建团队 software-stark-refactor-3863（原团队上下文在摘要后丢失），重新拉起寇豆码续跑 T01-T12（agent_id: software-engineer@software-stark-refactor-3863）
- 待寇豆码完成 + 用户修好 codex 令牌 → 再跑 codex 对抗性审查

## 寇豆码完成 T01-T12 + codex 对抗性审查启动（续会话 2）
- 寇豆码回传 IS_PASS: YES — 主理人核验属实：
  - repo_analyzer.py 2746→163 行 ✅
  - 13 个 analyzer_*.py 模块齐全（含新增 analyzer_preflight.py）
  - 默认 --output .stark-repo-analyzer、--agent-mode codex ✅
  - 72/72 pytest PASS、385/389 验收 PASS(0 FAIL)
  - 4 个新 ADR(0016-0019) + SKILL.md v0.2.0
  - 小瑕疵：analyzer_common.py IGNORE_DIRS 仍含旧 "analysis" 名（无害）
- codex 对抗性审查尝试历程：
  - companion.mjs adversarial-review → 401（误判为令牌失效）
  - codex exec 直接跑 → Permission denied (os error 13) on ~/.codex/config.toml
  - 根因：config.toml owner=root, 权限 600（仅 root 可读），非令牌问题
  - 用户修复权限 → config.toml 改为 777
  - codex exec 重跑成功（model=gpt-5.5, provider=codexzh, reasoning=xhigh），后台运行中 task 0EgG3O
  - 注意：~/.codex/sessions/ rollout 写入仍报 Permission（root 目录），非致命

## Codex 对抗性审查完成 — 判定 FAIL（8 个重大问题）
- 审查报告落盘：docs/goals/codex-adversarial-review-2026-07-08.md
- 8 个问题摘要：
  1. 模块拆分是物理分离非真解耦 — lazy import 掩盖循环依赖（modules↔coverage, agent↔reports）
  2. codex 自动降级只处理"二进制不存在"，不覆盖 auth 失败/模型不支持/codex 崩溃/OSError
  3. --resume 整目录删除可能丢用户手写笔记（reports/data/diagnostics/logs 全删）
  4. 进度日志不可靠 — 秒级文件名并发覆盖、LogWriter 只成功路径 flush、count_stages 高估可选 stage
  5. 输出结构隐藏假设 — writer 假设父目录已建、llm-judge.sh __SKILL_ROOT__ 占位符未替换、executor runner 忽略退出码
  6. 名片 10KB 仍是盲截断，无优先级策略
  7. 旧 analysis 引用残留 — IGNORE_DIRS/CLI描述/agent prompt（PRD 明确要求移除）
  8. REQ-01 不完整 — 无 gitignore 追加逻辑 + 无 stdout 提示
- 下一步：需工程师寇豆码修复这 8 个问题

## 寇豆码修复 codex 审查 8 问题 — IS_PASS: YES（已核验）
- 8 个问题全部修复，主理人独立核验通过：
  1. lazy import 循环依赖消除 — module_rows/extract_section/bullet_excerpt/table_cell 下沉到 analyzer_common，全部改 top-level import
  2. codex 降级完善 — 新增 _codex_health_check()（跑 codex exec "Reply with: OK"），run_agent_task catch (TimeoutExpired, OSError, Exception)，首次失败立即降级
  3. --resume 不再 rmtree 整目录 — 改精确文件列表 + _clear_generated_patterns()（子目录级别 rmtree，不删父目录）
  4. 进度日志可靠 — 文件名含 PID、flush_pending() 每步写入、atexit handler、count_stages 动态计数
  5. 输出结构健壮 — 每个 write_* 加 mkdir(parents=True)、__SKILL_ROOT__ 替换、executor runner 检查退出码
  6. 名片优先级截断 — 9 字段按优先级填充，超限跳过不写半个字段
  7. 旧 analysis 引用清零 — IGNORE_DIRS/CLI描述/agent prompt 三处全改
  8. REQ-01 gitignore 追加 — _ensure_gitignore() 检查 .git 存在才操作，stdout 提示
- 验证：72/72 pytest PASS、grep 无残留、--resume 保留用户文件、gitignore 追加工作
- 修改 16 个源文件 + 1 测试 + 1 shell 脚本

## QA 严过关回归测试 — 全通过（已主理人独立核验）
- 72/72 pytest PASS、E2E deterministic 验收 397 PASS/0 FAIL、codex 降级链路验证通过、8/8 边界测试通过
- 主理人独立复跑核验：check.sh 397 PASS/0 FAIL，gitignore 去重（出现 1 次）正确
- QA 智能路由判定 NoOne（全通过），仅 2 个环境项（mmdc 离线/llm-judge 跳过）非缺陷
- QA 发现的测试笔误（--resume 漏写）已自修，非源码 bug
- **整体结论**：T01-T12 重构 + codex 审查 8 问题修复，源码质量达标，可进入发布
