# 2026-07-07

## 规则优先级与执行 SOP 设计
- 用户要求先设计规则与规则优先级，形成可长期照做的 SOP，避免执行混乱。
- 产出：`docs/RULES-SOP.md`，核心内容：
  - 权威链 Tier0–6（安全硬规则 > 用户指令+hooks > 技能 > 仓库规则 > 记忆 > 全局设定 > 智能体）。
  - 仓库规则三级分类 MUST/SHOULD/MAY；MUST 只能由环境层（hook/sandbox/Tier0）兜底，聊天不可豁免。
  - 覆盖四规则：明确性、留痕、疑似误写先确认、MUST 不走聊天豁免。
  - 执行 SOP 八步 checklist + 冲突上升规则 + 本仓库规则清单（AGENTS.md/CLAUDE.md 实际规则已分级）。
- 关键结论：朴素"用户>仓库"会让护栏形同虚设；真护栏须下沉到 hook/sandbox，md 仅作软指引。
- 待用户确认是否进一步：把 AGENTS.md 规则显式标 MUST/SHOULD/MAY；新增 user-prompt-submit 提交钩做 MUST 级拦截。

## stark-repo-analyzer · run-005 UAT 复核 + PLAN_COMPLIANCE.md 更新
- 分析了 `uat-evidence/run-005`（`claude-video` 完整智能闭环 UAT）：18 阶段全 PASS；`acceptance/check.sh` 与裁判重跑 `acceptance-recheck-main.log` 均 245/245 PASS；8 模块覆盖率全 1.00；SLA 415.09s/30min；agent 占 96.5%（modules-batch 365.25s + cross-ref-review 35.17s，均 read-only、未超时）。
- 用 graphify 知识图谱（graph.json，commit 49d4d8bc）交叉复核差距分析：确认 tree-sitter 代码路径存在、repo-types/LLM-judge/ask_user 仅为文档设计未落地、repair 闭环已实装（7 代码函数+3 测试+repairs 产物）；修正"图无 run-005"误判——图含 run-005/check.sh 节点但**无 JUDGE.md 裁判节点**（run-001/002/003 有）。
- 已更新 `references/PLAN_COMPLIANCE.md`（Craft 模式）：
  - 口径说明与结论改以 run-005 为准；状态块新增 `完整智能闭环 UAT (run-005): PASS`，full PLAN 仍 PARTIAL。
  - 第 3 节 5 个 P0 项（模块 subagent / cross-ref / tree-sitter 覆盖率 / repomix 切片 / ADR-0011 硬断言）由"部分实现/仍需 claude-video UAT"升级为"已实现并验证（run-005）"。
  - 修正覆盖率引擎口径：run-005 用 regex-fallback 是本机无 tree-sitter CLI 的环境依赖，非功能缺失。
  - 修正 Codex 子代理参数口径：实测 `model_reasoning_effort="low"`、未带 --model（外层 gpt-5.5/xhigh），与文档原记 `--model 5.4`+medium 不一致，已标注待确认。
  - 新增第 5 节「run-005 UAT 证据更新」+ 最小补齐顺序 #4（补 run-005/JUDGE.md 人工 6 维度评分表）。
- 仍 open 的真实缺口：`ask_user()` 适配器、`config/repo-types.yaml` 外置、token 统计、LLM-judge/Mermaid/外链验收、外部调研 03-research、tree-sitter benchmark。

## stark-repo-analyzer · 未完成功能完整规划（PRD + 架构 + 任务拆解）
- 用户要求对 PLAN_COMPLIANCE.md 未实现功能做「从 spec 到任务拆解」完整规划（仅规划，不写代码）。
- 建团队 software-stark-unfinished，调度许清楚(PM)与高见远(架构师)产出增量 PRD + 增量架构设计。
- 交付物：`references/PRD_UNFINISHED_FEATURES.md`、`references/ARCH_UNFINISHED_FEATURES.md`、`references/class-diagram.mermaid`、`references/sequence-diagram.mermaid`。
- 7 项能力拆成 22 个有序任务 T01-T22；P1 三项（#1 ask_user 三运行时适配器 / #2 config/repo-types.yaml 外置 / #3 token 统计）全程并行；关键路径 T14(benchmark)→T13(真实冲突验证)→T12(acceptance 高成本项)→T15(run-005 JUDGE.md 六维)。
- 架构师据实修正两处偏差：① 不能用 PyYAML（SKILL.md「不装额外依赖」硬约束），改专用 RepoTypeLoader(stdlib) 解析受限 YAML 子集；② acceptance/check.sh 是 write_acceptance 生成的 heredoc（非 ADR-0011 设想的 5 文件），新增 3 执行器挂接汇总。另有：metadata.json 无 token 字段（用 <!-- TOKENS --> 标记解析）、render_report.py 无需改。
- 待用户拍板 Open Q：三运行时清单(Q1)、token 数据源(Q2)、benchmark 仓库选择(Q3)、LLM-judge 模型/预算(Q4)。
- 后续若实现：调度工程师(Kou)按 T 表写代码 + QA(严过关)测试（标准 SOP）。

## stark-repo-analyzer · 未完成功能实现（T01–T22）完成
- 工程师寇豆码按 T 表实现 7 项功能：新增 18 文件、改 `scripts/repo_analyzer.py`/`templates/*.tmpl.md`/`SKILL.md`/`references/PLAN_COMPLIANCE.md`；首报 69 测试 PASS、IS_PASS YES。
- QA 严过关独立验证（2 轮）：第1轮 SUCCESS_WITH_RESERVATIONS，抓到真实源码 Bug（T06 `run_agent_task` 在写 token 字段前就 `append(dict(metadata))` 到 `agent_attempts[]`，致 PERFORMANCE 报告缺 token 溯源）；第2轮工程师修复后 72/72 全绿（QA 新增 `tests/test_agent_token_integration.py` 3 条断言锁定修复）。
- 强验证点：T02 字节一致契约（QA 从 git HEAD 提旧 `SLICES` 作真值比对 6 类全一致）、T12 acceptance `TOTAL:` 汇总、T13 冲突 fixture 有效、T15 JUDGE.md 六维 5/5。
- 环境降级：T07 外部调研为**永久 SKIP stub**（PRD T07② 未达标，待用户拍板接受或补联网实现）；T11 LLM-judge / T14 benchmark 因沙箱断网仅实现代码+占位，真跑降级 SKIP/WARN。
- 遗留非阻断：T05 超时分支测试缺口、`TokenCollector.summary→as_dict` 命名漂移、T11 受众匹配度无阈值闸门。
- CI 注意：单次 `unittest discover tests` 在本沙箱易 OOM(SIGKILL 137)，需分模块运行或加内存上限。
