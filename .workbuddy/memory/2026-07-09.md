# 2026-07-09

## 用户痛点 + 对抗性 Review（stark-repo-analyzer）
- 任务：分析仓库解决的用户痛点，并做对抗性 review 是否真解决。
- 三大声称痛点（对标 yzddmr6/repo-analyzer）：P1 工序空跑 → 动态切片降维；P2 模型自评 → 符号级覆盖率硬门控；P3 单报告不通吃 → 3 份受众报告+可重放。
- 审查结论（已核实源码）：方向对、兑现打折。
  - P1 部分解决：repo-types.yaml 确实按类型给 5/6/7 维（fallback 12），但空切片未消除、类型识别无验收、version 绑定硬编码为 1。
  - P2 形式上解决/实质未解决：覆盖率门控度量的是"符号名是否出现在草稿文本(\b name \b)"，不是"被真正分析"；core=前3模块(按文件数排序)≥0.8；冷门语言因符号提取退化易满分。
  - P3 半成品：4 个模板章节结构真分化，但引用的全是重构前旧路径（02a-manifest-card.md / 05-module-ids.yaml / drafts/06- / 08-coverage.md / slices/ / --output analysis），代码已迁到 data/、diagnostics/slices/、diagnostics/module-drafts/ → 渲染报告内部链接全断。根因：acceptance/04-link.sh 只查外链不查内部相对链接。确定性模式下 agent_insights 返回空 → 三份报告塌缩为同一确定性骨架。
  - 附带：report_body()/overview_report_body() 已成死代码（write_reports 走 report_data+模板）；llm_judge DEFAULT_MODEL 已修为空（codex 审查#3已闭环）。
- 交付物：docs/adversarial-review-painpoints-2026-07-09.md
- 优先级建议：P0 修模板链接+P3 验收内部链接；P1 强化覆盖率度量+受众差异化兜底；P2 类型识别加验收+清死代码。
