# subagent 不可用时必须取得用户同意

运行环境不支持 subagent 时，skill 不得自动改为主 Agent 顺序分析；它必须暂停并告知用户能力缺失及耗时影响，让用户明确选择继续顺序分析或停止本次分析。用户选择继续时，源码阅读覆盖率、交叉验证和报告质量合同保持不变，只有执行方式从并行变为顺序。

## 影响

- 顺序执行不是失败，但必须在运行记录和最终报告限制中明确披露。
- 用户没有确认前不能启动模块深度分析，也不能把未发生的并行调度记为完成。
- subagent 能力缺失不能作为降低覆盖率或省略模块分析的理由。
