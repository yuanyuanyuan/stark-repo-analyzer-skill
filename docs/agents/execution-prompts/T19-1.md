# 执行 Prompt · ticket T19-1

- 生成时间：2026-07-11 20:39 CST
- requirement_ref：docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md（parent_issue: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/19）
- ticket_id：T19-1
- tickets_path：docs/agents/tickets/issue-19.md
- 生成器：requirement-dev-generator-prompt（Stage A）→ 完整执行 Prompt
- 目标业务仓库：/Users/chuzu/projests/stark-repo-analyzer-skill/spec-v2.2-standard-deep-modes-and-rules-based-to
- acceptance_env 来源：用户确认按 Stage A 建议补齐 → C local-cli 标准块（未编造外部 URL）
- uat_runner / 授权：agent + session-user（用户同意「按建议补齐」；执行会话若用户否认授权 → 仅 draft + UAT: blocked）
- 说明：本文件可复制到**新会话**在业务仓库根目录执行；**生成回合不执行主路径**。
- 本票摘要：Insight Probe 结构化产物（`insight-probes.json` schema + Catalog 三类 id）与 Process Gate（`insight-probe-process`）；`miss` 不挡门；不实现 LLM 判定；不改 T19-4 的 gate 红写终稿行为。

---

# Role
你是本仓库「单票交付执行器」：在 Codex 上按固定主路径完成 **一张** 已保存 ticket 的实现与 PR 交付，并遵守 `$goalpro` / `$meta-theory` / `$planning-with-files` / `$implement` 的嵌套合同与本编排覆写规则。

工作目录必须是**目标业务仓库**（不是 tools-config-guide，除非业务就在该仓）：

`/Users/chuzu/projests/stark-repo-analyzer-skill/spec-v2.2-standard-deep-modes-and-rules-based-to`

# 本票参数（已写死）

- requirement_ref: docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md
  - parent_issue: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/19
- tickets_path: docs/agents/tickets/issue-19.md
- ticket_id: T19-1
- acceptance_env:
  ```text
  形态: local-cli
  入口: 业务仓库根目录（本工作区）；CLI 入口 bin/repo-analyzer.js（npm bin: repo-analyzer）
  依赖: Node.js >=20；npm test / npm run typecheck；Git；文本搜索（rg 优先，否则 grep）；
        fixture / 夹具工件按 ticket test_plan（insight-probes.json、quality-gate-report 相关）
  数据: test/fixtures 或本票实现时新建的 gate 夹具工件；默认不造生产数据
  就绪标准: 在仓库根执行 npm test 退出码 0；node bin/repo-analyzer.js --help（或等价）可执行；
            与 T19-1 相关的 gate/schema/文档路径可打开对照 AC
  禁止: 分析过程不修改被分析仓库源码；不实现 LLM 探针判定逻辑本身（本票仅机械 schema+gate）；
        不把 status=miss 做成失败门禁；不把 allowed_to_synthesize 语义改成「是否写任何终稿」
        （那是 T19-4）；不连生产；不为 memory 单独开 PR；不宣称 Beats-v1.0（三包未齐）
  ```
- uat_runner: agent
- uat_authorized_by: session-user（Stage A 用户确认按建议补齐 local-cli；授权 agent 在 acceptance_env 内代跑 UAT/机械核对。执行会话若用户否认授权 → 仅 draft + UAT: blocked）
- base_branch: （默认探测 main，否则 master，再否则硬停）
- branch_prefix: feat/（默认；本票 branch 建议 `feat/T19-1`）
- allow_meta_degraded: false
- allow_failed_pr: false
- ignore_blockers: false
- draft_pr: （默认跟 UAT/测试规则；测绿 + 授权 UAT/机械核对过 → 正式 PR）

## 本票工作摘要（执行时以 tickets 正文为准）

- title: Insight Probe 结构化产物与 Process Gate
- blocked_by: none
- package: P1（机械层）
- what_to_build（压缩）:
  - 约定并文档化 `insight-probes.json` 最小 schema
  - Catalog 三类 id：`ui_promise_runtime_path`、`multi_source_rules`、`config_dual_write_dead_impl`
  - `quality-gate-report.json` 增加 `insight-probe-process`（或等价）检查
  - 缺文件/缺类别/非法 status/字段 → fail；合法 miss 全集不因探针 fail
  - 不实现 LLM 判定；不改变 T19-4 交付写稿语义
- acceptance_criteria: 见 tickets 内 T19-1 勾选项（≥6 条）
- test_plan:
  - unit: 扩展 gate 夹具（有/无 probes、缺类别、非法 status、三类 miss）+ `npm test`（及 typecheck）
  - e2e: N/A 或轻量 prepareArtifacts + gate CLI 断言
  - uat: N/A（纯机械门）；真实 UAT 留待 T19-5 — 本编排仍须在 PR body 写 UAT 节：机械 AC 核对步骤 + 观察

# 主路径（固定）
已保存 tickets
  → $goalpro（Goal + Loop）
  → G1（审 Goal + Loop）
  → **显式启动 $planning-with-files**（本票 run 进度 SSOT；见 §3.0）
  → $meta-theory（managed 全链路调度；阶段推进必须回写 planning files）
       内含：干净树检查 → 新建 feature branch → owner 持 $implement 写码
       → code-review → meta Review/Meta-Review → Verification
       → UAT → G3 → push + gh pr create/更新
  → 强制 Loop 轮（PR 开出后；硬停未开 PR 则仅失败复盘；同步收束 planning files）

禁止主路径出现：$to-spec、$to-tickets、$to-issues、publish_tracker、自动发 GitHub Issue。
PR 可 Refs/链接 requirement_ref 与 parent_issue #19，但不得创建新 issue。
本轮只做 **T19-1**；禁止顺手实现 T19-2/3/4/5。

政策登记：/Users/chuzu/projests/tools-config-guide/docs/agents/requirement-dev-orchestrator.md

---

## 0. 记忆枢纽（防再犯 SSOT）
- 路径：docs/agents/memory/
  - INDEX.md（总台账）
  - lessons.md（只追加）
  - 可选 run-log.md
- 权威序：ticket + 测试/PR 证据 > **本票 planning files** > docs/agents/memory > meta state > Codex 会话记忆
- meta memory strategy：project_only
- **职责边界（强制）**：
  - `$planning-with-files`（本票 run）：阶段进度、发现、断点续跑 → `.planning/ticket-T19-1/`
  - `docs/agents/memory/`（跨票）：防再犯 lessons；**不是**本 run 进度条
  - 禁止只用 memory/聊天代替 planning files 的阶段状态
  - 禁止把跨票教训只写在 findings 而不进 memory（有可复用经验时仍要 lessons+INDEX）
- Evolution（产品票）：none-with-reason: out_of_scope_for_ticket_pr_pipeline
  - 写 lessons ≠ writeback agent/SOUL
- 目录不存在：自动 scaffold 空壳 INDEX + lessons，报告「已初始化」
- **执行前必读**：INDEX.md + 与本票相关的 lessons；输出 ≤3 条避坑要点
  - 禁止「靠会话记忆免读文件」
- 有可复用经验：收工前追加 lessons + 更新 INDEX「最近一条」
- 与业务改动同一 feature branch / 同一 PR 提交（可 chore(memory) 分 commit）
- 硬停且不开 PR：仍可本地写 memory；不自动 push/不为 memory 单独开 PR；报告写明路径

脏树例外：若 git status **仅有**下列路径则不触发「无关脏树」硬停：
- `docs/agents/execution-prompts/**`
- `.planning/**`（本票 planning-with-files 工作区；含 `.active_plan`）
其它改动 → 硬停。
（进入执行后按规则提交的 memory、planning files 与业务文件属于本票工作，不再用「启动前仅 prompts/planning」例外掩盖无关脏文件。）

---

## 1. 入口硬闸门
1. 缺 requirement_ref / tickets_path / ticket_id / acceptance_env → 硬停。
2. tickets 无法解析、无此 id、缺最低字段/test_plan 三节 → 硬停。
3. 多票但未唯一指定 ticket_id → 硬停（本轮只跑 **T19-1**）。
4. blocked_by 非 none 且 ignore_blockers ≠ true → 硬停。（T19-1 为 none，可通过）
5. 不自动拆票、不改写 tickets 正文。

Ticket 最低字段：id, title, what_to_build, acceptance_criteria(≥1), blocked_by, test_plan{unit,e2e,uat}。

本票已校验（生成时）：id=T19-1；blocked_by=none；test_plan 含 unit/e2e/uat 三节。

---

## 2. Phase A — $goalpro
- 显式运行 $goalpro，产出 **Goal Prompt + Loop Prompt**。
- Goal 材料应锚定 T19-1 AC：schema 文档化、三类 Catalog id、gate 检查项、miss 不挡门、单测夹具、**排除** LLM 判定与 T19-4 写稿语义。
- **G1（人确认）**：两段都审。
  - 原生选择面优先（request_user_input）；否则 Decision Card，**等待回复**。
  - 禁止超时自动选推荐。
- G1 否决或 goalpro 无法收敛 → 硬停：不建 branch、不 implement、不进 Phase B。
- Goal 进入后续 Critical 材料；Loop 的时间参数/复盘约定一并锁定（本编排「PR 后立刻 Loop」视为本轮触发，不创建系统定时任务）。

G1 同时吸收 meta Critical 意图锁定；勿再弹空泛 IntentCard（除非缺改路线维度）。

---

## 3. Phase B — $meta-theory（managed）+ Weapon 合同

### 3.0 显式启动 $planning-with-files（强制 · 进度追踪 SSOT）

G1 通过后、进入 meta Execution / 建 branch **之前**，必须完成下列动作。这是编排硬合同，不是可选优化。

#### 3.0.1 启动
1. **显式运行 / 加载** `$planning-with-files`（或等价：按该 skill 规则创建并维护三文件）。禁止只在报告里写技能名。
2. **恢复优先**：若本票 plan 目录已存在 → 先读 `task_plan.md` + `progress.md` + `findings.md`，再继续；禁止无读盘直接重开新进度叙事。
3. **初始化路径（业务仓，隔离多票）**：
   - 目录：`.planning/ticket-T19-1/`
   - 三文件：`task_plan.md` · `findings.md` · `progress.md`
   - 激活指针：写/更新 `.planning/.active_plan` → 指向上述目录（相对路径即可）
   - 禁止默认把三文件甩在仓库根（避免多票互相覆盖）；若用户明确要求 root legacy 模式，须在 findings 记录原因
4. 目录不存在可创建。启动瞬间仅新增/修改 `.planning/**` 与既有 dirty 例外路径时，不算无关脏树。

#### 3.0.2 本票默认阶段骨架（写入 task_plan.md）
可用同义拆分，但必须覆盖且可勾选状态 `pending | in_progress | complete`：

| Phase | 含义 | 完成证据（示例） |
|-------|------|------------------|
| P0 Restore | 读 ticket / memory 相关 lessons / 既有 planning | ≤3 避坑要点已写入 progress 或 findings |
| P1 Goalpro+G1 | Goal+Loop 产出且 G1 通过 | G1 结果写入 findings；Current Phase 离开 P1 |
| P2 Meta route+G2 | managed 路由；G2 或 no_branching_choice | owner+weapon+verification 写入 task_plan Decisions |
| P3 Branch+Implement | feature branch + owner 持 $implement | branch 名、关键 commits 写入 progress |
| P4 Review+Verify+UAT | code-review + meta R/MR + 测 + UAT | 命令/摘录索引写入 progress（完整证据仍进 PR body） |
| P5 G3+PR | 开/更 PR | PR URL + body 证据节齐备标记 |
| P6 Loop | 强制 Loop + memory 更新 + planning 收束 | Done/Pause/Next 写入 progress；task_plan 终态 |

#### 3.0.3 更新节奏（update-only）
- **只追加 / refinement / 标记 superseded**；禁止在 active run 中整文件清空重写三文件
- 每完成一阶段：`task_plan.md` 状态 `in_progress→complete`，`Current Phase` 前进
- 任一发现/路线分叉/矛盾：写入 `findings.md`（含 G1/G2/G3 决策摘要）
- 动作、测结果、错误：写入 `progress.md`（会话日志）
- 2-Action 规则：连续 2 次探索性读/搜后，关键结论落盘 findings，禁止只留在聊天
- 阶段用户可见通知仍 ≤3 bullet；**聊天不能替代**三文件

#### 3.0.4 与 git / PR
- 建 branch 后：planning 更新默认随本票 feature branch 提交（建议 `chore(T19-1): planning progress` 或与业务同 commit 说明）
- 硬停且不开 PR：planning 可只留本地；报告写明 `.planning/ticket-T19-1/` 路径
- PR body「Review / Meta」节增加一行：`planning-with-files: .planning/ticket-T19-1/`（或 N/A+硬停原因——仅当未启动且已 hard-stop 前失败）

#### 3.0.5 合格 / 不合格
- **合格**：三文件存在于隔离目录；G1 后已启动；阶段状态与真实 git/PR 不矛盾；断会话可只靠三文件+git 续跑
- **不合格（managed 糊弄信号）**：只提技能名无文件；文件停在 P0/P1 却已声称 implement/PR；用 Todo/聊天/memory 顶替阶段状态；root 与多票 plan 互相覆盖且无说明

### 3.1 Weapon 合同（强制）
- 调度器：meta-theory（DISPATCHER）
- **连续性武器（强制）**：`$planning-with-files`（本票进度 SSOT；§3.0）
- 实现武器：$implement（TDD、常 typecheck/单测、结束全量测、code-review、**在当前 feature 分支 commit**）
- 执行 owner：可调用的实现向工人/execution owner
  - 找不到 → 硬停（除非 allow_meta_degraded: true → 主线程加载 $implement，PR/报告标 meta-theory: degraded + implement: main-thread）
- 禁止：「meta-theory 自己改业务代码」的表述与行为（非 degraded）
- meta 各阶段推进时，**conductor/主协调线程**负责回写 planning files（除非 run 显式委派写入 owner，并在 task_plan 记录）

### 3.2 managed 判定（行为证据）
- 成功：有路由/阶段推进，且 branch→implement→验证→PR 发生在合同内，**并且** `.planning/ticket-T19-1/` 三文件按阶段更新（至少 Current Phase 与最近 progress 条目匹配真实进度）
- 不可用：进不了 Phase B / blocked / 仅技能名或仅 hook_observed/status JSON 无推进 → 硬停（除非 allow_meta_degraded）
- **无 planning 文件或进度与交付事实明显脱节 → 视为糊弄，不得报 managed 成功**
- 糊弄 ≠ 成功

### 3.3 闸门映射
- G1 = Critical（已完成）
- G2 = Thinking 路径确认（Execution 前唯一路线确认）
  选项固定：① 按本票合同执行（推荐）② 停 ③ 改范围（停，人改 ticket）
  卡片写明 owner + weapon=$implement + verification
  真技术分叉才加选项；否则 no_branching_choice 由 G2 吸收
- G3 = Verification/UAT 后的开 PR 决策
- meta Review 仅流程质量且不改范围 → 不弹人

### 3.4 建 branch 前
- 无关脏工作区 → 硬停（execution-prompts 例外见上）
- 目标 branch 已存在（本地/远端）→ 硬停
- 不自动 stash、不删 branch
- base_branch：参数或 main→master→硬停
- 新 branch：`{branch_prefix}{safe(ticket_id)}` → `feat/T19-1`（safe 仅 [a-zA-Z0-9._-]）
- 从最新 origin/<base_branch> 拉出（可 fetch）

### 3.5 Execution 时间线
0) （若尚未做）§3.0 启动/恢复 planning-with-files；P2 路由+G2 写入 plan
1) owner + $implement 写码/自测/全量测 → 更新 progress（P3）
   - 实现焦点：schema 文档、Catalog 三类 id、gate `insight-probe-process`、夹具单测
   - 禁止范围：LLM 探针判定、T19-4 Synthesis/Degraded 写稿、T19-5 金样
2) **强制 code-review**；高/阻断 finding → 硬停，不开 PR（findings 记 finding 摘要）
3) meta Review / Meta-Review（默认不弹人）→ progress/findings 记结论
4) meta Verification：新鲜 unit + 适用 e2e → progress 记命令索引
5) UAT（本票机械核对：按 AC 检查 schema 文档存在、gate 行为、夹具结果；test_plan.uat 为 N/A 时 PR 仍写详细机械 UAT 步骤）→ progress 记步骤索引
6) G3 → push/PR → task_plan P5 complete；progress 写 PR URL

Commit：`feat(T19-1): <短句>`（或 fix/docs/chore；拿不准 feat）
PR 标题同形。PR body 建议 Refs #19。

### 3.6 测试与 e2e
- unit/e2e **红** → 硬停，默认不 push/不开 PR（除非 allow_failed_pr: true → draft + tests: failed，且 PR body 仍须贴**失败摘录与复现命令**）
- 有 e2e 能力 → 必须跑；证据进 PR body（见 3.8.1），禁止只写「e2e passed」
- 无 e2e → PR 写 `## Test Evidence — E2E (N/A)`：**理由 + 替代证据的命令/摘录/范围**；禁止沉默跳过/伪造
- 本票 test_plan：e2e 可为 N/A 或轻量 gate CLI；若 N/A，替代证据至少含 `npm test` 夹具相关用例摘录

### 3.7 UAT
- acceptance_env 字段说明与合格线：`/Users/chuzu/projests/tools-config-guide/docs/agents/acceptance-env-fill-guide.md`
- acceptance_env 无效/缺失（含仅「本地/staging/待定」）→ 不跑 UAT，只允许 draft；报告中提示用户按填写指南补全
- 真人授权 = 执行参数 uat_runner: agent + uat_authorized_by
- 有授权：agent 可代跑，通过则算真实 UAT，可正式 PR；**步骤与观察必须写入 PR body UAT 节**
- 无授权：仅 agent_preflight → draft + UAT: blocked（PR body 写清未授权与 preflight 做了什么）
- 测绿 + UAT 失败 → push + draft + UAT: failed（PR body 写失败步骤与现象）
- 测绿 + 授权 + UAT 过 → push + 正式 PR（PR body UAT 节仍要详细步骤，不得只写「UAT 通过」）
- 本票 ticket 写 uat N/A：执行器仍须按 AC 做**机械验收核对**（文件/schema/gate 输出），在 UAT 节写清「产品 UAT N/A 理由 + 机械核对步骤与观察」

### 3.8 Git/PR 权限
允许：checkout -b、commit、push -u、gh pr create / gh pr edit（按规则 --draft）
禁止：merge、删远端 branch、强推、改保护规则
无 gh/权限 → 降级报告 blocker，并把**本应写入 PR 的完整 body 草稿**输出到聊天与 `docs/agents/execution-prompts/T19-1.pr-body.md` 便于人粘贴

一票一 branch 一 PR；本轮不做第二张票。

### 3.8.1 PR message / description：详细测试证据合同（强制）

开 PR 或更新 PR 时，**body（PR message）必须**让 reviewer **不翻聊天记录也能复核**。聊天里的「测过了」**不能**替代 PR body。

#### 禁止（不合格证据）
- 仅：`unit: pass` / `all tests green` / `e2e ok` / `UAT 通过`
- 无命令、无用例范围、无日志摘录、无复现方式
- 用「见 CI」一笔带过且不给 job 名/链接/关键失败或通过日志

#### 必须结构（标题建议固定，便于 review）

```markdown
## Summary
- ticket_id / 标题
- requirement_ref
- 行为变更一句话

## Scope
- 本票做了什么 / 明确不做的

## Test Evidence — Unit
- **Command:** （完整可复制）
- **Working directory:**
- **Scope / cases:** （文件、describe/it 名、或 test_plan.unit 对应项）
- **Result:** （通过/失败计数或关键断言）
- **Log excerpt:** （关键原文 10–40 行，含 pass/fail）
- **Full log path:** （若另存文件则写路径）
- **How to re-run:**
- **When / branch / runtime:** （时间、分支、可选运行时版本）

## Test Evidence — E2E
（有 e2e 时：字段同 Unit，可加浏览器/环境）
（无 e2e 时：标题改为 `## Test Evidence — E2E (N/A)`）
- **Why N/A:**
- **Substitute evidence:** （替代命令 + 摘录 + 范围，不得空白）

## Test Evidence — UAT
- **acceptance_env:** （摘要或贴标准块）
- **Authorized by / runner:** （人或 agent + uat_authorized_by）
- **Steps:** （按 AC 逐步，1. 2. 3.）
- **Observed results:** （每步实际现象）
- **Artifacts:** （截图/录屏/HAR 路径；没有则写「无截图，步骤可按上表复现」）
- **Verdict:** pass | failed | blocked + 一句原因

## Review / Meta
- code-review：高危/建议级摘要
- meta-theory: normal | degraded
- planning-with-files: `.planning/ticket-T19-1/`（或路径）
- lessons 链接（若有）

## Risk / Rollback
- 已知风险与回滚方式
```

#### 合格线（G3）
- 正式 PR：Unit 节合格 +（E2E 合格或 E2E N/A+替代合格）+ UAT 节 pass 且步骤具体
- draft（UAT blocked/failed 或 allow_failed_pr）：仍须填齐已有部分的**命令/摘录/失败细节**，并标明缺口
- 证据不合格 → G3 **不得**选「正式可合并」；应补跑补写 body 或停

#### 写入方式
- `gh pr create --title "..." --body "$(cat <<'EOF' ... EOF)"` 或等价；更新时用 `gh pr edit --body`
- Loop 小修 push 后：**重跑验证并改写** Test Evidence 各节，删除过期结论

### 3.8.2 G3 检查清单（给人看）
展示给用户确认前，执行器应自检 PR body 是否含 Unit/E2E/UAT 三节且每节含 command + excerpt（或 N/A 替代细节）。缺则先补 body 再弹 G3。

### 3.9 G2 否决
硬停；不改 tickets；不建 branch。

---

## 4. 强制 Loop 轮
- 触发：PR 已开出（正式或 draft）后 **自动**进入
- 必做：对照 PR/测试/UAT 写差距；更新 memory lessons+INDEX；**回写 planning**（P6：差距、Done/Pause/Next、task_plan 终态）；Next LOOP packet 或 Done/Pause
- 无需改代码：不弹人，直接收束（progress 标 complete / pause）
- 需要同 PR 小修：先确认 修 / 只记教训不修 / 停
  - 禁止自动小修 push；禁止新 scope/新票/第二 PR/大重构
- 若确认「修」：
  - 同 branch 改代码；planning 回到对应 phase（通常 P3/P4），**禁止**假装 P6 已结束
  - **完整**重走 Verification + UAT + **G3**
  - 更新 **同一 PR** body 与状态（**按 3.8.1 刷新详细 Test Evidence 各节**）；不关旧开新
  - 每步同步 progress.md
- 主路径硬停未开 PR：Loop = 失败复盘，写 memory + **planning（失败阶段/blocker/下一步）** + 报告

---

## 5. 用户可见沟通
- 中文；阶段通知短（≤3 条 bullet）
- 闸门：原生选择面 → 失败则 Decision Card，必须等待
- managed/degraded/blocker 必须说清楚
- 开跑与关键阶段切换时，可点名 planning 路径一句（例：进度在 `.planning/ticket-T19-1/`）；不把三文件全文贴进聊天

---

## 6. 完成定义（本流水线 userGoalDone）
以下之一：
- 正式 PR 已开/已更新，**PR body 含 3.8.1 合格详细测试证据**，Loop 轮已完成（含 memory），**且** planning 三文件反映终态（P6 complete 或明确 Pause + 下一步）
- draft PR 已开/已更新，正文诚实标注状态且**已有部分证据仍具体（命令/摘录/失败细节）**，Loop 轮已完成，**且** planning 终态诚实（含 draft/blocked 原因）
- 硬停：报告原因 + 本地 memory（若有）+ **planning 中的 blocker/当前 phase/下一步** + 路径，无虚假完成

---

## 7. 停止条件摘要
材料/闸门/managed/脏树/已有 branch/红测（默）/code-review 高危/G1·G2 否决 → 硬停并报告。
禁止假治理、假 UAT、假 e2e、假 writeback。
禁止 PR body 仅用「pass/全绿/已测」冒充测试证据。
禁止本回合扩大到 T19-2+ 或宣称 Beats-v1.0。
