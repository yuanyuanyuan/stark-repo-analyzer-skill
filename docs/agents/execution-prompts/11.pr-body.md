## Summary
- ticket_id: 11 · Spec: v2.2 standard/deep modes and rules-based tool usage
- requirement_ref: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11
- 行为变更：去掉 quick；仅 standard（默认）/deep；local rules SSOT；doctor 能力矩阵 + 安装 prompt；deep 缺能力拒绝不降级；报告/gate 元数据对齐

## Scope
- CLI/budgets/gate/doctor/units 模式合同
- `skills/repo-analyzer/rules/**` 官方源元数据
- SKILL/README/CHANGELOG 与测试
- 明确不做：分析 run 内装工具、deep→standard 静默降级、新建第二张票

## Test Evidence — Unit
- **Command:** `npm test`
- **Working directory:** 仓库根
- **Scope / cases:** doctor 三态、CLI 模式门禁、gate 去 quick、rules 官方源、units standard 启发式、deep 不降级、e2e standard/deep 链路（38 tests）
- **Result:** 38 pass / 0 fail
- **Log excerpt:**
```
ℹ tests 38
ℹ pass 38
ℹ fail 0
ℹ duration_ms 21084.177083
```
- **Full log path:** `/tmp/full-test2.out`（本地验证）
- **How to re-run:** `npm test`
- **When / branch / runtime:** 2026-07-11；分支 `yuanyuanyuan/spec-v2.2-standard-deep-modes-and-rules-based-to`；Node `node -v` 见 CI/本地

- **Command:** `npm run typecheck`
- **Result:** exit 0（`node --check` on bin/src）

## Test Evidence — E2E
- **Command:** `node --test test/e2e.test.js`（含于 `npm test`）
- **Scope:** standard-only 全链路元数据；Graphify-fixture deep 全链路 + gate dual profiles
- **Result:** pass（代表性仓库 e2e + standard-only e2e）
- **Log excerpt:** 见 `npm test` 中 `✔ standard-only 全链路…` / `✔ 代表性仓库完成 v2 全链路…`
- **How to re-run:** `npm test`

## Test Evidence — UAT
- **acceptance_env:** local-cli（业务仓根；bin/repo-analyzer.js；Node>=20）
- **Authorized by / runner:** session-user / agent
- **Steps:**
  1. CLI usage 仅 standard/deep，无 quick 支持承诺
  2. doctor standard-only：available=[standard]，blocked 含 deep
  3. standard scan+units：enumerator=heuristic-text，limitations 披露；不依赖增强工具
  4. deep doctor/units 缺能力：非零退出；无 ANALYSIS_REPORT
  5. install-prompt 含缺失能力与「不得修改被分析仓源码」
  6. rules/tools/*.json 均含 official_source_url + verified_date
  7. budget/gate 元数据含 rules_version / mode
  8. `npm test` 基线绿
- **Observed results:** 上述步骤均 PASS（本地脚本，2026-07-11）
- **Artifacts:** 临时目录 `/tmp/repo-analyzer-uat-*`（可复现命令见上）
- **Verdict:** pass

## Review / Meta
- code-review：未发现阻断级问题；注意 gate 不再二次 deep 能力门禁（有意）
- meta-theory: managed（实现 owner=$implement；用户 G2 覆写不建 feat/11）
- planning-with-files: `.planning/ticket-11/`
- lessons: `docs/agents/memory/lessons.md`

## Risk / Rollback
- standard units 为启发式，覆盖率语义与 deep 不同——依赖 limitations 披露
- Graphify 真实二进制能力可能与 fixture mock 有差异；deep 放行仍以能力矩阵为准
- 回滚：revert commit `feat(11): standard/deep modes and rules-based tool usage`
