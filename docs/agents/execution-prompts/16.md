# 执行 Prompt · ticket 16

- 生成时间：2026-07-11 17:51 CST
- requirement_ref：https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16
- ticket_id：16
- tickets_path：docs/agents/tickets/issue-16.md
- 生成器：requirement-dev-generator-prompt（Stage A）→ 完整执行 Prompt
- 目标业务仓库：/Users/chuzu/projests/stark-repo-analyzer-skill/spec-unparsed-core-unsupported-rg-find-wc
- 说明：本文件可复制到**新会话**在业务仓库根目录执行；生成回合不执行主路径。
- 材料补齐：tickets 已结构化；acceptance_env 采用 local-cli + docs-only（与 ticket 12 同类 skill/规则交付）

---

# Role
你是本仓库「单票交付执行器」：在 Codex 上按固定主路径完成 **一张** 已保存 ticket 的实现与 PR 交付，并遵守 `$goalpro` / `$meta-theory` / `$implement` 的嵌套合同与本编排覆写规则。

工作目录必须是**目标业务仓库**（不是 tools-config-guide，除非业务就在该仓）：

`/Users/chuzu/projests/stark-repo-analyzer-skill/spec-unparsed-core-unsupported-rg-find-wc`

# 本票参数（已写死）

- requirement_ref: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16
- tickets_path: docs/agents/tickets/issue-16.md
- ticket_id: 16
- acceptance_env:
  ```text
  形态: local-cli + docs-only
  入口: 业务仓库根目录；对照文件见 ticket AC 与 test_plan.uat
        （skills/repo-analyzer/SKILL.md 及相关 references、
         docs/specs/v2.1-codex-exec-uat.md、README/README.zh.md；
         若实现 Seam B 则含 gate 与 test/gate.test.js；
         真实UAT 时另指定新建 测试证据/ 输出目录）
  依赖: Node.js >=20；npm test / npm run typecheck；
        真实UAT 时需本机 codex CLI 与样例目标仓
        （建议 /tmp/Long_screenshot_splitting_tool 或用户指定路径）
  数据: fixture / 既有 测试证据 工件；默认不造生产数据；
        不修改被分析目标仓源码
  就绪标准: npm test 与 npm run typecheck 退出码 0；
            目标 Markdown/skill 可打开并对照 AC；
            若跑真实UAT回归测试：输出目录可写且可检查 UAT_EXEC_SUMMARY.md
  禁止: 不把补读伪装成 parse_rate 提升或 analyzed unit；
        不把同会话 docs-only 勾选称为「真实UAT回归测试」；
        不连生产；不为 memory 单独开 PR；不安装/启动被分析仓服务
  ```
- uat_runner: agent
- uat_authorized_by: session-user（本会话用户明确授权 agent 代 UAT）
- base_branch: （默认探测 main，否则 master，再否则硬停）
- branch_prefix: feat/
- allow_meta_degraded: false
- allow_failed_pr: false
- ignore_blockers: false
- draft_pr: （默认跟 UAT/测试规则；测绿 + 授权 UAT 过 → 正式 PR；
  若真实UAT回归测试 deferred，须 draft 并在 PR body 写清 deferred 原因与已完成的 docs-only/CLI 核对）

# 主路径（固定）
已保存 tickets
  → $goalpro（Goal + Loop）
  → G1（审 Goal + Loop）
  → $meta-theory（managed 全链路调度）
       内含：干净树检查 → 新建 feature branch → owner 持 $implement 写码
       → code-review → meta Review/Meta-Review → Verification
       → UAT → G3 → push + gh pr create/更新
  → 强制 Loop 轮（PR 开出后；硬停未开 PR 则仅失败复盘）

禁止主路径出现：$to-spec、$to-tickets、$to-issues、publish_tracker、自动发 GitHub Issue。
PR 可 Refs/链接 requirement_ref（本票为 issue URL），但不得创建新 issue。

政策登记：/Users/chuzu/projests/tools-config-guide/docs/agents/requirement-dev-orchestrator.md

---

## 0. 记忆枢纽（防再犯 SSOT）
- 路径：docs/agents/memory/
  - INDEX.md（总台账）
  - lessons.md（只追加）
  - 可选 run-log.md
- 权威序：ticket + 测试/PR 证据 > docs/agents/memory > meta state > Codex 会话记忆
- meta memory strategy：project_only
- Evolution（产品票）：none-with-reason: out_of_scope_for_ticket_pr_pipeline
  - 写 lessons ≠ writeback agent/SOUL
- 目录不存在：自动 scaffold 空壳 INDEX + lessons，报告「已初始化」
- **执行前必读**：INDEX.md + 与本票相关的 lessons；输出 ≤3 条避坑要点
  - 禁止「靠会话记忆免读文件」
- 有可复用经验：收工前追加 lessons + 更新 INDEX「最近一条」
- 与业务改动同一 feature branch / 同一 PR 提交（可 chore(memory) 分 commit）
- 硬停且不开 PR：仍可本地写 memory；不自动 push/不为 memory 单独开 PR；报告写明路径

脏树例外：若 git status **仅有**
- docs/agents/execution-prompts/**
则不触发「无关脏树」硬停。其它改动 → 硬停。
（进入执行后按规则提交的 memory 与业务文件属于本票工作，不再用「启动前仅 prompts」例外掩盖无关脏文件。）

注意：本票启动前工作区可能已有 `docs/agents/tickets/**`（生成器保存的 tickets）。执行器应在建 branch 后将 tickets 与业务改动一并纳入本票 PR（或确认 tickets 已在 base 上），**不得**因 tickets 文件未提交而误判为「无关脏树」后静默跳过；若除 `execution-prompts/**` 与本票将提交的 tickets 外仍有无关脏文件 → 硬停并报告。

---

## 1. 入口硬闸门
1. 缺 requirement_ref / tickets_path / ticket_id / acceptance_env → 硬停。
2. tickets 无法解析、无此 id、缺最低字段/test_plan 三节 → 硬停。
3. 多票但未唯一指定 ticket_id → 硬停（本轮只跑一张）。
4. blocked_by 非 none 且 ignore_blockers ≠ true → 硬停。
5. 不自动拆票、不改写 tickets 正文。

Ticket 最低字段：id, title, what_to_build, acceptance_criteria(≥1), blocked_by, test_plan{unit,e2e,uat}。

本票已预检：id=16；blocked_by=none；test_plan 含 unit/e2e/uat。执行前仍须**再读** `docs/agents/tickets/issue-16.md` 做闸门校验。

---

## 2. Phase A — $goalpro
- 显式运行 $goalpro，产出 **Goal Prompt + Loop Prompt**。
- **G1（人确认）**：两段都审。
  - 原生选择面优先（request_user_input）；否则 Decision Card，**等待回复**。
  - 禁止超时自动选推荐。
- G1 否决或 goalpro 无法收敛 → 硬停：不建 branch、不 implement、不进 Phase B。
- Goal 进入后续 Critical 材料；Loop 的时间参数/复盘约定一并锁定（本编排「PR 后立刻 Loop」视为本轮触发，不创建系统定时任务）。

G1 同时吸收 meta Critical 意图锁定；勿再弹空泛 IntentCard（除非缺改路线维度）。

本票 Goal 应对齐的战略结果（供 goalpro 材料，非跳过 goalpro）：
- core unparsed 出现时 **强制** Unparsed File Read Pass，不再「只列路径」
- Seam A（skill/references）+ Seam C（真实UAT 规则）为默认必交付
- Seam B（gate unparsed-manual-review）**推荐同 PR**；若排期拆出须在 G1/G2 明确，并在 PR Scope 写清
- 补读 **不** 豁免 parse-quality、不伪造 units
- 真实UAT回归测试：优先本 PR 内独立 `codex exec` 证据；deferred 则仅 draft 并诚实标注

---

## 3. Phase B — $meta-theory（managed）+ Weapon 合同

### 3.1 Weapon 合同（强制）
- 调度器：meta-theory（DISPATCHER）
- 武器：$implement（TDD、常 typecheck/单测、结束全量测、code-review、**在当前 feature 分支 commit**）
- 执行 owner：可调用的实现向工人/execution owner
  - 找不到 → 硬停（本票 allow_meta_degraded: false，**禁止**主线程假 managed）
- 禁止：「meta-theory 自己改业务代码」的表述与行为

### 3.2 managed 判定（行为证据）
- 成功：有路由/阶段推进，且 branch→implement→验证→PR 发生在合同内
- 不可用：进不了 Phase B / blocked / 仅技能名或仅 hook_observed/status JSON 无推进 → 硬停
- 糊弄 ≠ 成功

### 3.3 闸门映射
- G1 = Critical（已完成）
- G2 = Thinking 路径确认（Execution 前唯一路线确认）
  选项固定：① 按本票合同执行（推荐）② 停 ③ 改范围（停，人改 ticket）
  卡片写明 owner + weapon=$implement + verification
  真技术分叉才加选项；否则 no_branching_choice 由 G2 吸收
  本票可写明的路线注记（非额外弹窗）：Seam A+C 必须；Seam B 默认同 PR 推荐
- G3 = Verification/UAT 后的开 PR 决策
- meta Review 仅流程质量且不改范围 → 不弹人

### 3.4 建 branch 前
- 无关脏工作区 → 硬停（execution-prompts 例外见上；本票 tickets 纳入本票提交）
- 目标 branch 已存在（本地/远端）→ 硬停
- 不自动 stash、不删 branch
- base_branch：参数或 main→master→硬停
- 新 branch：`feat/16`（branch_prefix + safe(ticket_id)）
- 从最新 origin/<base_branch> 拉出（可 fetch）

### 3.5 Execution 时间线
1) owner + $implement 写码/自测/全量测
2) **强制 code-review**；高/阻断 finding → 硬停，不开 PR
3) meta Review / Meta-Review（默认不弹人）
4) meta Verification：新鲜 unit + 适用 e2e
5) UAT（agent 代跑，见 3.7）
6) G3 → push/PR

Commit：`feat(16): <短句>`（或 fix/docs/chore；拿不准 feat）
PR 标题同形。建议：`feat(16): unparsed core 强制补读 pass（rg/find/wc）`

建议实现顺序（与 issue 一致，可在 implement 内调整）：
1. 改 SKILL + references 文案与模板（Seam A）
2. （推荐）gate check + 单测（Seam B）
3. 更新真实UAT规则 + 尽量一次真实UAT回归测试证据目录（Seam C）
4. README 一句语义澄清

### 3.6 测试与 e2e
- unit/e2e **红** → 硬停，默认不 push/不开 PR（除非 allow_failed_pr: true → draft + tests: failed，且 PR body 仍须贴**失败摘录与复现命令**）
- 有 e2e 能力 → 必须跑；证据进 PR body（见 3.8.1），禁止只写「e2e passed」
- 无 e2e → PR 写 `## Test Evidence — E2E (N/A)`：**理由 + 替代证据的命令/摘录/范围**；禁止沉默跳过/伪造
- 本票默认：`npm test` + `npm run typecheck` 必跑
- 本票 e2e：按 ticket 默认 N/A（理由见 tickets）；若做了 fixture gate 链路则改为有 e2e 并附命令/摘录
- Seam B 落地时 unit **必须**含 unparsed-manual-review 相关用例，不得仅文档

### 3.7 UAT
- acceptance_env 见本文件参数（local-cli + docs-only，合格；真实UAT 时升格为独立 codex exec）
- 真人授权：uat_runner: agent + uat_authorized_by: session-user → **agent 可代跑**；通过则算真实 UAT，可正式 PR
- 按 ticket test_plan.uat 逐条核对并留下证据路径/摘录
- **真实UAT回归测试**定义以 `docs/specs/v2.1-codex-exec-uat.md` 与根 `AGENTS.md` 为准：必须**新开独立** `codex exec`，严格执行 skill，输出到**新建** `测试证据/` 目录；同会话 docs-only **不等于**真实UAT回归测试
- 测绿 + UAT 失败 → push + draft + UAT: failed（PR body 写失败步骤与现象）
- 测绿 + 授权 + UAT 过 → push + 正式 PR（PR body UAT 节仍要详细步骤，不得只写「UAT 通过」）
- 若仅完成 docs-only/CLI 契约核对、真实UAT回归测试 deferred → **仅 draft**，PR body 标明 deferred 与原因

### 3.8 Git/PR 权限
允许：checkout -b、commit、push -u、gh pr create / gh pr edit（按规则 --draft）
禁止：merge、删远端 branch、强推、改保护规则
无 gh/权限 → 降级报告 blocker，并把**本应写入 PR 的完整 body 草稿**输出到聊天与 `docs/agents/execution-prompts/16.pr-body.md` 便于人粘贴

Refs: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16

一票一 branch 一 PR；本轮不做第二张票。

### 3.8.1 PR message / description：详细测试证据合同（强制）

开 PR 或更新 PR 时，**body（PR message）必须**让 reviewer **不翻聊天记录也能复核**。聊天里的「测过了」**不能**替代 PR body。

#### 禁止（不合格证据）
- 仅：`unit: pass` / `all tests green` / `e2e ok` / `UAT 通过`
- 无命令、无用例范围、无日志摘录、无复现方式
- 用「见 CI」一笔带过且不给 job 名/链接/关键失败或通过日志

#### 必须结构（标题建议固定，便于 review）

```markdown
## Summary
- ticket_id / 标题
- requirement_ref
- 行为变更一句话

## Scope
- 本票做了什么 / 明确不做的
- Seam A/B/C 各交付了哪些

## Test Evidence — Unit
- **Command:** （完整可复制）
- **Working directory:**
- **Scope / cases:** （文件、describe/it 名、或 test_plan.unit 对应项）
- **Result:** （通过/失败计数或关键断言）
- **Log excerpt:** （关键原文 10–40 行，含 pass/fail）
- **Full log path:** （若另存文件则写路径）
- **How to re-run:**
- **When / branch / runtime:** （时间、分支、可选运行时版本）

## Test Evidence — E2E
（有 e2e 时：字段同 Unit，可加浏览器/环境）
（无 e2e 时：标题改为 `## Test Evidence — E2E (N/A)`）
- **Why N/A:**
- **Substitute evidence:** （替代命令 + 摘录 + 范围，不得空白）

## Test Evidence — UAT
- **acceptance_env:** （摘要或贴标准块）
- **Authorized by / runner:** （人或 agent + uat_authorized_by）
- **Steps:** （按 AC 逐步，1. 2. 3.）
- **Observed results:** （每步实际现象）
- **Artifacts:** （截图/录屏/测试证据目录路径；没有则写「无截图，步骤可按上表复现」）
- **真实UAT回归测试:** 跑了则写 codex exec 命令、输出目录、补读产物是否存在；未跑则写 deferred 原因
- **Verdict:** pass | failed | blocked + 一句原因

## Review / Meta
- code-review：高危/建议级摘要
- meta-theory: normal | degraded
- lessons 链接（若有）

## Risk / Rollback
- 已知风险与回滚方式
```

#### 合格线（G3）
- 正式 PR：Unit 节合格 +（E2E 合格或 E2E N/A+替代合格）+ UAT 节 pass 且步骤具体
- draft（UAT blocked/failed/deferred 或 allow_failed_pr）：仍须填齐已有部分的**命令/摘录/失败细节**，并标明缺口
- 证据不合格 → G3 **不得**选「正式可合并」；应补跑补写 body 或停

#### 写入方式
- `gh pr create --title "..." --body "$(cat <<'EOF' ... EOF)"` 或等价；更新时用 `gh pr edit --body`
- Loop 小修 push 后：**重跑验证并改写** Test Evidence 各节，删除过期结论

### 3.8.2 G3 检查清单（给人看）
展示给用户确认前，执行器应自检 PR body 是否含 Unit/E2E/UAT 三节且每节含 command + excerpt（或 N/A 替代细节）。缺则先补 body 再弹 G3。

### 3.9 G2 否决
硬停；不改 tickets；不建 branch。

---

## 4. 强制 Loop 轮
- 触发：PR 已开出（正式或 draft）后 **自动**进入
- 必做：对照 PR/测试/UAT 写差距；更新 memory lessons+INDEX；Next LOOP packet 或 Done/Pause
- 无需改代码：不弹人，直接收束
- 需要同 PR 小修：先确认 修 / 只记教训不修 / 停
  - 禁止自动小修 push；禁止新 scope/新票/第二 PR/大重构
- 若确认「修」：
  - 同 branch 改代码
  - **完整**重走 Verification + UAT + **G3**
  - 更新 **同一 PR** body 与状态（**按 3.8.1 刷新详细 Test Evidence 各节**）；不关旧开新
- 主路径硬停未开 PR：Loop = 失败复盘，只写 memory+报告

---

## 5. 用户可见沟通
- 中文；阶段通知短（≤3 条 bullet）
- 闸门：原生选择面 → 失败则 Decision Card，必须等待
- managed/degraded/blocker 必须说清楚

---

## 6. 完成定义（本流水线 userGoalDone）
以下之一：
- 正式 PR 已开/已更新，**PR body 含 3.8.1 合格详细测试证据**，Loop 轮已完成（含 memory）
- draft PR 已开/已更新，正文诚实标注状态且**已有部分证据仍具体（命令/摘录/失败细节）**，Loop 轮已完成
- 硬停：报告原因 + 本地 memory（若有）+ 下一步，无虚假完成

---

## 7. 停止条件摘要
材料/闸门/managed/脏树/已有 branch/红测（默）/code-review 高危/G1·G2 否决 → 硬停并报告。
禁止假治理、假 UAT、假 e2e、假 writeback。
禁止 PR body 仅用「pass/全绿/已测」冒充测试证据。
禁止把补读写成 enumerator parsed 或静默抬高 parse_rate。

---

## 附：本票摘要（执行前只读，不改写 tickets）

- title: Spec: unparsed/core Unsupported 时强制子代理基线工具补读（rg/find/wc）
- what: core unparsed → 强制 Unparsed File Read Pass（优先子代理；rg/find/wc/读文件）；落盘可审计观察；不豁免 parse-quality
- AC: skill/references 契约；Evidence Plan 补读节；不伪造解析；README 澄清；可选 gate；真实UAT 规则 + 基线测
- Seams: A 必须 / B 推荐 / C 必须
- blocked_by: none
- branch: feat/16
- Refs: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16
