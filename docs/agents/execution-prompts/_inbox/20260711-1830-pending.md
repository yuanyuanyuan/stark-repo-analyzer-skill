# 补齐材料说明 · Stage A 生成器

- 生成时间：2026-07-11 18:30 CST
- requirement_ref：https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11
- 状态：**未生成完整执行 Prompt**（必填字段不全 / tickets 未落盘）
- 目标业务仓库（当前 cwd）：`/Users/chuzu/projests/stark-repo-analyzer-skill/spec-v2.2-standard-deep-modes-and-rules-based-to`
- 生成器：`requirement-dev-generator-prompt`（Stage A）
- 政策：缺任一项不得假装可开跑主路径

---

## 已解析的 requirement_ref

| 项 | 值 |
|----|-----|
| Issue | [#11 Spec: v2.2 standard/deep modes and rules-based tool usage](https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11) |
| state | OPEN |
| label | `ready-for-agent` |
| 摘要 | 去掉 quick；仅 standard/deep；standard=可移植基线工具且禁用 Graphify/Ctags/ast-grep；deep=能力门禁（图查询/符号枚举/引用边），缺能力拒绝且不降级；引入可分发 local rules + doctor 能力矩阵 + 安装 prompt + 报告元数据 |

issue 正文含 Problem / Solution / User Stories / Implementation / Testing / Out of Scope，**规格完整**；但编排合同要求的是 **已保存本地 tickets 文件**，不是仅有 GitHub issue。

---

## 必填硬合同检查

| 字段 | 状态 | 说明 |
|------|------|------|
| `requirement_ref` | 合格 | issue #11 URL 已解析 |
| `tickets_path` | **缺失** | 用户未给；业务仓仅有 `docs/agents/tickets/issue-12.md`、`issue-16.md`，**无 issue-11 对应 tickets** |
| `ticket_id` | **缺失** | 用户未指定本轮唯一 id；不可默认假定 `11` 存在于 tickets |
| `acceptance_env` | **缺失/不合格** | 用户未提供；禁止把「本地/staging/待定」当合格 |

结论：**走补齐材料路径** → 本文件 + 聊天说明；**不**写入 `docs/agents/execution-prompts/<ticket_id>.md`。

---

## 还缺什么（请用户补齐）

### 1. tickets_path + ticket_id

期望：本地已有 tickets 文件/目录，且其中存在唯一 `ticket_id`，最低字段：

```text
id, title, what_to_build, acceptance_criteria(≥1), blocked_by, test_plan
test_plan 必须含三节：unit / e2e / uat
  - 可写 N/A + 一句理由
  - 缺任一节 = 无效
blocked_by 非 none 且未 ignore_blockers:true → 硬停
```

**期望格式示例**（可对齐本仓 `docs/agents/tickets/issue-12.md` 结构）：

```markdown
# Ticket

- id: "11"
- title: "v2.2 standard/deep modes and rules-based tool usage"
- requirement_ref: "https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11"
- source_issue: "https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11"
- labels: ready-for-agent
- blocked_by: none
- saved_from: "GitHub Issue #11 body + Stage A 结构化补全（test_plan）"
- saved_at: "<ISO8601>"

## what_to_build
（从 issue 压缩成可执行交付描述）

## acceptance_criteria
1. …
2. …

## test_plan

### unit
- …

### e2e
- … 或 N/A — 理由：…

### uat
- …
```

建议落盘路径（与现有约定一致）：

- `docs/agents/tickets/issue-11.md`  
  或拆多票时：`docs/agents/tickets/issue-11/` 下多文件 + 本轮指定一个 `ticket_id`

**生成器本回合禁止**：调用 `$to-spec` / `$to-tickets` / `$to-issues` / `publish_tracker` 自动拆单或改 issue。  
请你**事先**准备好本地 tickets（人写或另开会话拆单），再回来填：

```text
tickets_path: docs/agents/tickets/issue-11.md
ticket_id: 11
```

（`ticket_id` 以文件内 `id` 字段为准，不一定等于 issue 号。）

### 2. acceptance_env（UAT 环境）

还缺 `acceptance_env`（UAT 在哪个环境验收）。

请直接回复 **A/B/C/D/E**，并补最少信息：

- **A local-fullstack**：启动命令？前端/API 地址或端口？
- **B staging**：完整 URL？如何登录？
- **C local-cli**：验收命令？
- **D docs-only**：验收对照哪些文件？
- **E 自己写一行**：必须含入口 + 启动/已有 URL

填写模板见：

`/Users/chuzu/projests/tools-config-guide/docs/agents/acceptance-env-fill-guide.md`

**标准块模板（补齐后生成器会写入执行 Prompt）：**

```text
acceptance_env:
  形态: <local-fullstack | staging | preview | local-cli | docs-only | 其他>
  入口: <URL 或本地地址/端口>
  依赖: <服务/VPN/账号；无则写「无」>
  数据: <测试账号/种子；无则写「无」>
  就绪标准: <一句>
  禁止: <边界；无则写「无」>
```

**对本 issue 的合理默认（仅建议，生成器不替你编造为已填）：**

本仓库是 CLI / skill（repo-analyzer），issue #11 的测试决策偏 **CLI 可见行为、doctor 能力矩阵、报告工件、gate**。常见合格写法是 **C local-cli**，可选叠 **D docs-only**（rules 文件与报告元数据对照）。示例（你确认后才算数）：

```text
acceptance_env:
  形态: local-cli
  入口: 业务仓库根目录（本工作区）
  依赖: Node.js（见 package.json / README）；npm test / 项目 CLI（bin）；可选 Graphify/Ctags/ast-grep 仅用于 deep 能力矩阵场景
  数据: test/fixtures 或测试用最小仓库路径（按 ticket test_plan 写明）
  就绪标准: npm test 退出码 0；doctor / 模式相关 CLI dry-run 或 --help 可执行；deep 缺能力时失败路径可复现
  禁止: 分析过程不改被分析仓库源码；不为凑 deep 伪造能力；不把 standard 跑成 enhanced 工具路径
```

不合格示例（会再次打回）：`本地` / `staging` / `TBD` / 仅写「CLI」。

### 3. 可选参数（有则写，无则用默认）

```text
target_repo: （默认当前 cwd）
uat_runner: agent
uat_authorized_by: <你的名字或 session-user>
base_branch: （默认 main→master）
branch_prefix: feat/
allow_meta_degraded: false
allow_failed_pr: false
ignore_blockers: false
draft_pr: （默认跟 UAT/测试规则）
```

若要 agent 代跑 UAT 并冲**正式 PR**：必须同时有合格 `acceptance_env` + `uat_runner: agent` + `uat_authorized_by`。

---

## 补齐后如何重跑 Stage A

在业务仓库根目录新开（或继续）生成器会话，填写：

```text
requirement_ref: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/11
tickets_path: <你的 tickets 路径>
ticket_id: <文件内 id>
acceptance_env: <标准块或合格一行版>
uat_runner: agent
uat_authorized_by: <授权人>
```

齐套后生成器将：

1. 校验 ticket 最低字段与 `test_plan` 三节、`blocked_by`
2. 生成**完整执行 Prompt**（等价嵌入 `requirement-dev-execution-prompt-template.md` 全部政策）
3. 覆盖写入：`docs/agents/execution-prompts/<ticket_id>.md`
4. **强制**编入：`$planning-with-files`（§3.0）+ PR Test Evidence 详细证据合同（§3.8.1）
5. **不**在生成回合执行主路径

---

## 本回合明确不做

- 不跑 `$implement`、不改业务代码、不建 branch、不开 PR  
- 不调用 `$to-spec` / `$to-tickets` / `$to-issues` / `publish_tracker`  
- 不把 issue 正文伪装成已保存 ticket  
- 不编造 URL / 假 acceptance_env  
- 不写 `docs/agents/memory/` 正文  

---

## 停止

材料补齐说明已落盘。等待用户提供 `tickets_path`、`ticket_id`、合格 `acceptance_env` 后再生成完整执行 Prompt。
