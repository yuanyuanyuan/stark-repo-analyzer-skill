# 补齐材料说明 · 无法生成完整执行 Prompt

- 生成时间：2026-07-11 17:48 CST
- 生成器：requirement-dev-generator-prompt（Stage A）
- 目标：ticket 16 / Issue #16 Unparsed File Read Pass
- 结论：**必填未齐 → 本回合不生成可跑主路径执行 Prompt**
- 目标业务仓库：`/Users/chuzu/projests/stark-repo-analyzer-skill/spec-unparsed-core-unsupported-rg-find-wc`

---

## 用户提供的入口

| 字段 | 用户给出 | 状态 |
|------|----------|------|
| requirement_ref | https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16 | 有（issue 正文完整） |
| tickets_path | docs/agents/tickets/issue-16.md | 有路径，但**结构不合格** |
| ticket_id | （未显式；可推断 16） | 可补全为 `16` |
| acceptance_env | **未提供** | **不合格 / 缺失** |
| target_repo | 当前工作区（默认可） | 有 |

---

## 阻断项（按硬合同）

### 1. tickets 最低字段缺失

`docs/agents/tickets/issue-16.md` 当前仅为 Issue 指针 + 摘要，**缺少**执行闸门要求的：

- `id`
- `title`（可从 issue 抄，但票内未结构化）
- `what_to_build`
- `acceptance_criteria`（≥1 条）
- `blocked_by`
- `test_plan`，且必须含三节：
  - `unit`
  - `e2e`（可 N/A + 一句理由）
  - `uat`（可 N/A + 一句理由；本票通常应对真实UAT/CLI 写具体步骤）

生成器**禁止**调用 `$to-spec` / `$to-tickets` / `$to-issues` / `publish_tracker`，也**禁止**在本回合替你改写 tickets 正文。  
请在 `docs/agents/tickets/issue-16.md`（或同目录新文件）自行补成完整 ticket 后重跑 Stage A。

#### 建议最小骨架（示例，需你确认后写入 tickets）

```markdown
# Ticket

- id: "16"
- title: "Spec: unparsed/core Unsupported 时强制子代理基线工具补读（rg/find/wc）"
- requirement_ref: "https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16"
- source_issue: "https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16"
- labels: ready-for-agent
- blocked_by: none
- saved_from: "GitHub Issue #16 + unparsed-manual-read-pass-spec.md"
- saved_at: "YYYY-MM-DDTHH:MM:00+08:00"

## what_to_build

当 coverage-units 出现 core unparsed 时，强制 Unparsed File Read Pass（优先子代理；
基线只读工具 rg/find/wc/读文件）；落盘 unparsed-file-reviews 或
module-evidence.unparsed_manual_reads；报告可引用 manual-read 锚点；
不清除 unparsed、不豁免 parse-quality。默认交付 Seam A（SKILL/references）+ Seam C
（真实UAT规则）；Seam B gate unparsed-manual-review 推荐同 PR 或紧随。

## acceptance_criteria

1. SKILL/references 写明触发条件、工具白名单、选样优先级、产物路径、
   confidence=manual-read、与 parse-quality 边界。
2. Evidence Plan 有 Unparsed File Read Pass 节；core unparsed 非空时不得仅列路径结束。
3. 补读不改变 unparsed 列表语义、不提升 parse_rate、不伪造 units。
4. （若做 Seam B）gate unparsed-manual-review：core unparsed 非空且无补读记录 → fail。
5. 真实UAT条款：目标仓 core unparsed 非空时，输出目录须有补读产物；
   report 对高影响文件有 manual-read 锚点叙述。

## test_plan

### unit

- 若实现 Seam B：node:test 覆盖「有 unparsed 无补读记录 → fail / 有记录 → pass /
  补读不豁免 parse-quality」；执行 `npm test`。
- 若本票仅 skill/docs：unit 可 N/A + 理由；仍跑 `npm test` 作回归基线。

### e2e

- 可选：fixture 工件模拟 core unparsed + 有/无补读记录，跑 gate/doctor 链路。
- 默认仅契约时：e2e: N/A — 理由：主验收为 skill 契约 + 真实UAT；fixture e2e 可选。

### uat

在 acceptance_env 下：

1. 对照 AC 检查 SKILL/references 文案与产物约定。
2. 若有 gate：用 fixture 验证 unparsed-manual-review。
3. 按 docs/specs/v2.1-codex-exec-uat.md 对新输出目录做真实UAT回归检查
   （core unparsed 非空时须有 unparsed-file-reviews* 或等价字段）。
4. 基线：`npm test` 与 `npm run typecheck` 退出码 0。
```

Issue 正文与 `docs/specs/unparsed-manual-read-pass-spec.md` 可作内容源；**写入后字段名须可解析**。

### 2. acceptance_env 缺失（必填）

未提供合格 `acceptance_env`。仅写「本地 / staging / 待定」无效。

#### 还缺 acceptance_env（UAT 在哪个环境验收）

请直接回复 **A/B/C/D/E**，并补最少信息：

- **A local-fullstack**：启动命令？前端/API 地址或端口？
- **B staging**：完整 URL？如何登录？
- **C local-cli**：验收命令？（本票推荐候选：仓库根目录 + `npm test` / 真实UAT codex exec 命令）
- **D docs-only**：验收对照哪些文件？（可与 C 组合：skill 文案 + 可选 CLI）
- **E 自己写一行**：必须含入口 + 启动/已有 URL

标准块模板：

```text
acceptance_env:
  形态: <local-fullstack | staging | preview | local-cli | docs-only | 其他>
  入口: <URL 或本地地址/端口>
  依赖: <服务/VPN/账号；无则写「无」>
  数据: <测试账号/种子；无则写「无」>
  就绪标准: <一句>
  禁止: <边界；无则写「无」>
```

填写指南：  
`/Users/chuzu/projests/tools-config-guide/docs/agents/acceptance-env-fill-guide.md`

#### 针对 Issue #16 的合理建议（**未写入执行 Prompt，仅供你选型**）

本票是 skill 契约 + 可选 gate + 真实UAT，通常类似 ticket 12：

```text
acceptance_env:
  形态: local-cli + docs-only
  入口: 业务仓库根目录；对照 SKILL/references、gate 单测（若有）、
        真实UAT 输出目录（如 测试证据/v2.1-human 或新建 run）
  依赖: Node.js >=20；npm test / npm run typecheck；
        真实UAT 时需 codex exec + 目标样例仓（如 /tmp/Long_screenshot_splitting_tool）
  数据: fixture 工件或既有 测试证据；默认不造生产数据
  就绪标准: npm test 与 npm run typecheck 退出码 0；目标文档可打开对照 AC；
            若跑真实UAT，输出目录可写且含 UAT_EXEC_SUMMARY.md 检查口径
  禁止: 不把补读伪装成 parse_rate 提升；不改目标仓源码；
        不声称完整 UAT 却无补读产物；不为 memory 单独开 PR
```

若你确认 **C+D** 或粘贴上块，生成器下一回合可写入执行 Prompt。

可选一并确认：

- `uat_runner: agent` + `uat_authorized_by: <你的名字>`（要正式 PR 时）
- 或仅 draft：不写 agent 授权

---

## 已读只读材料（本回合）

1. requirement：GitHub Issue #16 全文（Problem / Solution / Implementation Decisions / Testing / Out of Scope）
2. 本地：`docs/agents/tickets/issue-16.md`（摘要指针，不齐）
3. 本地：`docs/specs/unparsed-manual-read-pass-spec.md`（与 issue 对齐）
4. 生成器依赖：`requirement-dev-execution-prompt-template.md`、`acceptance-env-fill-guide.md`
5. 参照样例：`docs/agents/tickets/issue-12.md` + `docs/agents/execution-prompts/12.md`

---

## 下一回合怎么重跑 Stage A

齐料后，在同一生成器会话（或新会话贴生成器 prompt）提供：

```text
requirement_ref: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/16
tickets_path: docs/agents/tickets/issue-16.md
ticket_id: 16
acceptance_env:
  <标准块>
uat_runner: agent   # 可选
uat_authorized_by: <名字>  # agent 代 UAT 时
target_repo: /Users/chuzu/projests/stark-repo-analyzer-skill/spec-unparsed-core-unsupported-rg-find-wc
```

齐后生成器将：

- 聊天贴**完整执行 Prompt**
- 覆盖写入：`docs/agents/execution-prompts/16.md`
- 内嵌执行模板全部政策 + PR Test Evidence 详细证据合同

---

## 本回合明确未做

- 未生成可跑主路径执行 Prompt
- 未改 tickets / 业务代码 / 建 branch / 开 PR
- 未调用 to-spec / to-tickets / to-issues
