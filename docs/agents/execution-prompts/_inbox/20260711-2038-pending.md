# 补齐材料说明 · Stage A 生成器

- 生成时间：2026-07-11 20:38 CST
- requirement_ref：docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md（parent_issue: https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/19）
- 状态：**未生成完整执行 Prompt**（必填字段不全）
- 目标业务仓库（当前 cwd）：`/Users/chuzu/projests/stark-repo-analyzer-skill/spec-v2.2-standard-deep-modes-and-rules-based-to`
- 生成器：`requirement-dev-generator-prompt`（Stage A）
- 政策：缺任一项不得假装可开跑主路径
- 用户输入：仅指向生成器提示词 + `docs/agents/tickets/issue-19.md`（未写死 ticket_id / acceptance_env）

---

## 已解析的材料

| 项 | 值 |
|----|-----|
| tickets 文件 | [`docs/agents/tickets/issue-19.md`](../../tickets/issue-19.md) |
| parent issue | [#19](https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/19) |
| 本地规格 | [`docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md`](../../../specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md) |
| labels | ready-for-agent |
| 范围 | 第一交付切片 P1+P2+P3；P4 不在本批 |
| 建议顺序 | T19-1 →（T19-2 ∥ T19-4）→ T19-3 → T19-5 |

tickets 文件内含 **5 张票**（T19-1 … T19-5），每张均含 `id/title/what_to_build/acceptance_criteria/blocked_by/test_plan{unit,e2e,uat}`。  
编排合同是 **本轮只跑唯一 `ticket_id`**，不能把整份 issue-19 多票文件当成单票执行。

### 各票 blocked_by 速查

| ticket_id | title | blocked_by | 当前能否开跑（若选定） |
|-----------|-------|------------|------------------------|
| T19-1 | Insight Probe 结构化产物与 Process Gate | none | 可（无 blocker） |
| T19-2 | 候选枚举与 Skill 探针义务 | T19-1 | 否，除非 `ignore_blockers: true` 或 T19-1 已完成 |
| T19-3 | 探针 hit 必须进入终稿风险与改造优先级 | T19-2 | 否 |
| T19-4 | Full/Degraded 同路径终稿与 Synthesis Rule | T19-1 | 否，除非 T19-1 完成或 ignore |
| T19-5 | Beats-v1.0 验收锁：金样、Rubric、Agent Smoke | T19-3, T19-4 | 否 |

frontier 建议：先生成并执行 **T19-1**。

---

## 必填硬合同检查

| 字段 | 状态 | 说明 |
|------|------|------|
| `requirement_ref` | 合格（可推断） | tickets 内写明 `docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md`；文件存在可读。也可写 parent issue URL。 |
| `tickets_path` | 合格（可推断） | 用户给出的 `docs/agents/tickets/issue-19.md` |
| `ticket_id` | **缺失** | 文件含 T19-1…T19-5 五张票；用户未指定本轮唯一 id。**禁止默认猜一张。** |
| `acceptance_env` | **缺失/不合格** | 用户未提供；禁止把「本地/staging/待定」当合格 |

结论：**走补齐材料路径** → 本文件 + 聊天说明；**不**写入 `docs/agents/execution-prompts/<ticket_id>.md`。

---

## 还缺什么（请用户补齐）

### 1. ticket_id（本轮唯一）

请指定 **一张** 要生成执行 Prompt 的票，例如：

```text
tickets_path: docs/agents/tickets/issue-19.md
ticket_id: T19-1
requirement_ref: docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md
```

（`requirement_ref` 也可用 `https://github.com/yuanyuanyuan/stark-repo-analyzer-skill/issues/19`。）

- 若选 T19-2 / T19-3 / T19-4 / T19-5 且对应 blocker 未完成：须显式 `ignore_blockers: true`，否则入口闸门硬停。
- 生成器本回合禁止调用 `$to-spec` / `$to-tickets` / `$to-issues` / `publish_tracker` 改写 tickets。

### 2. acceptance_env（UAT 环境）

还缺 `acceptance_env`（UAT 在哪个环境验收）。

请直接回复 **A/B/C/D/E**，并补最少信息：

- **A local-fullstack**：启动命令？前端/API 地址或端口？
- **B staging**：完整 URL？如何登录？
- **C local-cli**：验收命令？
- **D docs-only**：验收对照哪些文件？
- **E 自己写一行**：必须含入口 + 启动/已有 URL

填写模板见：

`/Users/chuzu/projests/tools-config-guide/docs/agents/acceptance-env-fill-guide.md`

**标准块模板（补齐后生成器会写入执行 Prompt）：**

```text
acceptance_env:
  形态: <local-fullstack | staging | preview | local-cli | docs-only | 其他>
  入口: <URL 或本地地址/端口>
  依赖: <服务/VPN/账号；无则写「无」>
  数据: <测试账号/种子；无则写「无」>
  就绪标准: <一句>
  禁止: <边界；无则写「无」>
```

**对本批票的合理默认（仅建议，生成器不替你编造为已填）：**

本仓库是 CLI / skill（repo-analyzer）。T19-1 偏机械 gate + fixture；T19-2–4 偏 skill/文档/交付路径；T19-5 含真实UAT/金样。常见合格写法是 **C local-cli**，文档契约可叠 **D docs-only**。示例（你确认后才算数）：

```text
acceptance_env:
  形态: local-cli
  入口: 业务仓库根目录（本工作区）；CLI 入口 bin/repo-analyzer.js（npm bin: repo-analyzer）
  依赖: Node.js >=20；npm test / npm run typecheck；fixture / 测试证据目录按 ticket test_plan
  数据: test/fixtures 或 ticket 规定的夹具工件（insight-probes.json / quality-gate-report 等）；默认不造生产数据
  就绪标准: 在仓库根执行 npm test 退出码 0；node bin/repo-analyzer.js --help（或等价）可执行；
            与本票相关的 gate/CLI/文档路径可打开对照 AC
  禁止: 分析过程不修改被分析仓库源码；不把 gate 红的 Degraded 终稿宣称为产品分析完整通过；
        不连生产；不为 memory 单独开 PR；P4 deep reference 债不得靠放宽门禁装绿
```

若某票 `test_plan.uat` 为 N/A（如 T19-1 纯机械门），仍须写合格 `acceptance_env`（环境描述）；UAT 节可按票写 N/A + 替代核对步骤，但 **字段本身不能空/TBD**。

不合格示例（会再次打回）：`本地` / `staging` / `TBD` / 仅写「CLI」。

### 3. 可选参数（有则写，无则用默认）

```text
target_repo: （默认当前 cwd）
uat_runner: agent
uat_authorized_by: <你的名字或 session-user>
base_branch: （默认 main→master）
branch_prefix: feat/
allow_meta_degraded: false
allow_failed_pr: false
ignore_blockers: false
draft_pr: （默认跟 UAT/测试规则）
```

若要 agent 代跑 UAT 并冲**正式 PR**：必须同时有合格 `acceptance_env` + `uat_runner: agent` + `uat_authorized_by`。

---

## 补齐后如何重跑 Stage A

在业务仓库根目录继续本会话或新开生成器会话，填写：

```text
requirement_ref: docs/specs/v2.2-beats-v1.0-insight-probes-delivery.to-spec.md
tickets_path: docs/agents/tickets/issue-19.md
ticket_id: T19-1
acceptance_env: <标准块或合格一行版>
uat_runner: agent
uat_authorized_by: <授权人>
```

齐套后生成器将：

1. 校验该票最低字段与 `test_plan` 三节、`blocked_by`
2. 生成**完整执行 Prompt**（等价嵌入 `requirement-dev-execution-prompt-template.md` 全部政策）
3. 覆盖写入：`docs/agents/execution-prompts/<ticket_id>.md`（如 `T19-1.md`）
4. **强制**编入：`$planning-with-files`（§3.0）+ PR Test Evidence 详细证据合同（§3.8.1）
5. **不**在生成回合执行主路径

---

## 本回合明确不做

- 不跑 `$implement`、不改业务代码、不建 branch、不开 PR  
- 不调用 `$to-spec` / `$to-tickets` / `$to-issues` / `publish_tracker`  
- 不把多票文件伪装成已选定单票  
- 不编造 URL / 假 acceptance_env  
- 不写 `docs/agents/memory/` 正文  
- 不开始 T19-* 实现  

---

## 停止

材料补齐说明已落盘。等待用户提供 **唯一 `ticket_id`** 与合格 **`acceptance_env`** 后再生成完整执行 Prompt。
